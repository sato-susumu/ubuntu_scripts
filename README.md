# ubuntu_scripts

```
sudo apt update
sudo apt upgrade -y
sudo apt install -y ssh git curl
mkdir -p ~/work
cd ~/work
git clone https://github.com/sato-susumu/ubuntu_scripts.git
cd ubuntu_scripts/
./just_for_me.sh
```
